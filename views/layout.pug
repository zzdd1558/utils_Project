doctype html
html
    head
        title= title
        link(rel='stylesheet', href='/stylesheets/style.css')
        script(type="text/javascript" src="/__bundles/common.bundle.js")
    body
        .container
            .row(style="margin-top:40px;")
                nav.navbar.navbar-default.navbar-fixed-top
                    h2(style="align: left") 유틸 Test Project
                        .col-sm-3.pull-right
                            select#type.form-control
                                option(value="회원가입" selected) 선택하세요
        block content
